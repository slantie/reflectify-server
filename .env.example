# Environment Variables for FastAPI Server

# Backend URL - URL of the Express.js backend server
BACKEND_URL=http://localhost:4000

# Service API Key - Used for service-to-service authentication with the Express backend
# Generate a secure key using: require('crypto').randomBytes(32).toString('hex')
# This must match the SERVICE_API_KEY configured on your Express backend
SERVICE_API_KEY=sk_service_1234567890abcdef1234567890abcdef_secure_random_key_here

# Production Examples:
# BACKEND_URL=https://backend.reflectify.live
# SERVICE_API_KEY=your_secure_generated_api_key_here
